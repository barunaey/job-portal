<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Register New Company</title>
   <style>
  /* General Reset and Base Styling */
  * {
    margin: 0;
    padding: 0;
    box-sizing: border-box;
  }

  body, html {
    font-family: Arial, sans-serif;
    background-color: #ffffff; /* White background */
    color: #0b1d3a; /* Navy text */
    display: flex;
    flex-direction: column;
    align-items: center;
    min-height: 100vh;
  }

  /* Page Title */
  h1 {
    margin-top: 3rem;
    font-size: 2.2rem;
    color: #0b1d3a;
    text-align: center;
  }

  /* Form Container */
  form {
    margin-top: 2rem;
    padding: 2rem;
    width: 90%;
    max-width: 500px;
    background: #0b1d3a;
    border-radius: 10px;
    box-shadow: 0px 8px 16px rgba(0, 0, 0, 0.1);
    display: flex;
    flex-direction: column;
    gap: 1.5rem;
  }

  /* Form Labels */
  label {
    font-size: 1.1rem;
    color: #ffffff;
  }

  /* Form Inputs */
  input[type="text"] {
    padding: 0.8rem;
    font-size: 1rem;
    color: #0b1d3a;
    background-color: #f4f7fc;
    border: 1px solid #2a4373;
    border-radius: 5px;
    outline: none;
    transition: border 0.3s ease;
  }

  input[type="text"]:focus {
    border-color: #3e5b99;
  }

  /* Submit Button */
  button[type="submit"] {
    padding: 0.8rem;
    font-size: 1.1rem;
    font-weight: bold;
    background: linear-gradient(135deg, #1a2f4d, #2a4373);
    color: #ffffff;
    border: none;
    border-radius: 5px;
    cursor: pointer;
    transition: background 0.3s ease, transform 0.2s ease;
  }

  button[type="submit"]:hover {
    background: linear-gradient(135deg, #2a4373, #1a2f4d);
    transform: scale(1.05);
  }
</style>

</head>
<body>

    <!-- Page Title -->
    <h1>Register New Company</h1>

    <!-- Registration Form -->
    <form action="/company/register" method="POST">
        <label for="companyName">Company Name:</label>
        <input type="text" id="companyName" name="companyName" required>

        <label for="address">Address:</label>
        <input type="text" id="address" name="address" required>

        <label for="contactInfo">Contact Info:</label>
        <input type="text" id="contactInfo" name="contactInfo" required pattern="^\d{10}$">

        <button type="submit">Register Company</button>
    </form>

</body>
</html>
